{% extends 'base.html.twig' %}
{% block body %}
    <!--For Page-->
    <div class="page">
        <!--For Row containing all card-->
        <div class="row">
            {% for article in allArticles %}
            <!--Card 1-->
            <div class="offset-1 col-2">
                <div class="card card-cascade card-ecommerce wider shadow mb-5 ">
                    <!--Card image-->
                    <div class="view view-cascade overlay text-center"> <img class="card-img-top" src="https://i.imgur.com/9VZGPJF.jpg" alt=""> <a>
                            <div class="mask rgba-white-slight"></div>
                        </a> </div>
                    <!--Card Body-->
                    <div class="card-body card-body-cascade text-center">
                        <!--Card Title-->
                        <h4 class="card-title"><strong><a href="">{{article.name}}</a></strong></h4> <!-- Card Description-->
                        <p class="card-text">{{ article.description }} </p>
                        <p class="price">{{ article.price }} €</p> <!-- Card Rating-->
                        <!--Card footer-->
                        <div class="card-footer">
                            <button class="btn btn btn-dark"> Ajouter au panier </button>
                        </div>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}